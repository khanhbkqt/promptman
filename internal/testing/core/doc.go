// Package core provides the TestRunner orchestrator that loads test scripts,
// matches test functions to requests via key patterns, executes lifecycle
// hooks in the correct order, and manages the dual timeout system.
package core
