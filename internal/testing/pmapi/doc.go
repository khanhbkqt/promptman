// Package pmapi implements the Postman-compatible pm.* API surface
// that test scripts use inside the goja sandbox. It provides pm.test,
// pm.expect, pm.response, pm.environment, pm.variables, pm.assert,
// pm.sendRequest, and pm.timeout.
package pmapi
