// Package environment [M2] manages environment files and variable resolution.
// It handles variable scoping (request > collection > env), secret resolution from OS env,
// and active environment selection.
package environment
